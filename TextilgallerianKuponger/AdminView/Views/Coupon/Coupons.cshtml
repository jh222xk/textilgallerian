@model AdminView.ViewModel.CouponViewModel
@{
    ViewBag.Title = "Coupons";
}

<h2>Cupons</h2>

<div class="two wide column">

    <table class="ui table">
        <thead>
            <tr>
                <th>Kampanjnamn</th>
                <th>Kampanjkod</th>
                <th>Startdatum</th>
                <th>Slutdatum</th>
                <th>Typ av kampanj</th>
                <th>Skapad av</th>
                <th>Val</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var coupon in Model.Coupon1)
            {
                <tr>
                    <td>
                         @coupon.Code
                    </td>
                    <td>
                        @coupon.Code
                    </td>
                    <td>
                        @coupon.Start
                    </td>
                    <td>
                        @coupon.End
                    </td>
                    <td>
                        @coupon.Type
                    </td>
                    <td>
                        Linus
                    </td>
                    <td>
                        @Html.ActionLink("Ta bort", "Delete")

                        @Html.ActionLink("Redigera", "Edit")
                    </td>

                </tr>
            }


        </tbody>
    </table>
    <div class="ui hidden divider"></div>
    <div class="field">
    </div>
</div>
