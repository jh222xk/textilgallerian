@model Domain.Entities.Coupon

@{
    ViewBag.Title = "Ta bort kupong";
}


<h2>@Model.Name</h2>

<h3>Är du säker på att du vill ta bort kupongen?</h3>
<div>
<hr/>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-actions">

        <input type="submit" value="Ta bort" class="ui button red"/>
        @Html.HiddenFor(m => m.UniqueKey)
        @Html.ActionLink("Gå tillbaka", "Index", null, new {@class = "ui button blue"})
    </div>
}
</div>
